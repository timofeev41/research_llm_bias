# Метрики анализа смещения (Bias) в оценивании LLM моделей

## Обзор

Этот документ описывает метрики, используемые для анализа смещения (bias) в результатах оценивания, когда LLM модели оценивают ответы других LLM моделей. Анализ основан на данных из:
- **llm_forms/** - формы с текстами ответов моделей для каждой задачи
- **llm_self_rankings/** - ранжирования, которые дали LLM модели по различным критериям

## Структура данных

### Входные данные

1. **Формы (llm_forms/)** - CSV файлы с колонками:
   - `Task ID` - идентификатор задачи (A1, A2, B1, и т.д.)
   - `Category` - категория задачи (Idea-to-Text, Summarise, Interpret)
   - `Original` - оригинальный текст задачи
   - `Gemini`, `DeepSeek`, `Flash`, `Qwen` - тексты ответов каждой модели

2. **Ранжирования (llm_self_rankings/)** - CSV файлы с колонками:
   - `task_id` - идентификатор задачи
   - `task_type` - тип задачи
   - `model_name` - имя модели-оценивателя
   - `evaluator` - идентификатор оценивателя (A_V1, B_V2, и т.д.)
   - `Fluency`, `Coherence`, `Conciseness`, `Accuracy`, `Constructiveness`, `Final choice`, `Motivational tone`, `Sentiment match` - оценки по каждому критерию

### Система оценок

Каждая модель получает оценку по каждому критерию:
- **1.0** = 1-е место (4 балла)
- **0.6666666667** = 2-е место (3 балла)
- **0.3333333333** = 3-е место (2 балла)
- **0.0** = 4-е место (1 балл)

## Метрики

### 1. Model Bias (Смещение к моделям)

#### Описание
Средний балл каждой модели у конкретного LLM-оценивателя. Показывает, какую модель оцениватель склонен ставить выше других.

#### Расчет
Для каждой модели (Gemini, DeepSeek, Flash, Qwen) вычисляется средний балл по всем критериям и всем задачам:
```
model_bias_Model = среднее(всех баллов модели у оценивателя)
```

#### Поля в результатах
- `model_bias_Gemini` - средний балл модели Gemini
- `model_bias_DeepSeek` - средний балл модели DeepSeek
- `model_bias_Flash` - средний балл модели Flash
- `model_bias_Qwen` - средний балл модели Qwen

#### Интерпретация
- **Высокий балл (>2.5)** - оцениватель склонен ставить эту модель выше
- **Низкий балл (<2.0)** - оцениватель склонен ставить эту модель ниже
- **Близкие значения** - оцениватель относительно беспристрастен
- **Большая разница** - сильное смещение к определенным моделям

#### Пример
Если `model_bias_Qwen = 3.0` и `model_bias_Gemini = 2.0`, это означает, что оцениватель в среднем ставит Qwen на 1-2 места выше, чем Gemini.

---

### 2. Monotonicity Score (Монотонность)

#### Описание
Метрика проверяет, есть ли механический паттерн в оценивании - например, всегда ставить модели в одном и том же порядке независимо от качества ответов.

#### Расчет
Используется корреляция Спирмена между рангами моделей в последовательных задачах:
```
monotonicity_score = средняя корреляция(ранги_задачи_i, ранги_задачи_i+1)
```

#### Поле в результатах
- `monotonicity_score` - значение от -1 до 1

#### Интерпретация
- **> 0.7** - высокий риск механического паттерна (🚩 флаг `flag_mechanical_pattern`)
- **0.3 - 0.7** - умеренная консистентность в оценивании
- **< 0.3** - низкая консистентность, оцениватель меняет порядок моделей
- **< 0** - отрицательная корреляция, оцениватель меняет порядок на противоположный

#### Пример
Если `monotonicity_score = 0.8`, это означает, что оцениватель почти всегда ставит модели в одном и том же порядке, что может указывать на систематическое смещение, а не на объективную оценку качества.

---

### 3. Variance Score (Дисперсия)

#### Описание
Дисперсия всех выставленных оценок. Показывает, насколько оцениватель различает качество ответов.

#### Расчет
```
variance_score = дисперсия(всех баллов по всем критериям и задачам)
```

#### Поле в результатах
- `variance_score` - значение дисперсии

#### Интерпретация
- **< 1.0** - низкая дисперсия, оцениватель почти не различает качество (🚩 флаг `flag_low_variance`)
- **≈ 1.25** - нормальная дисперсия (ожидаемое значение при идеальном ранжировании)
- **> 1.5** - высокая дисперсия, оцениватель сильно различает качество

#### Пример
Если `variance_score = 0.5`, это означает, что оцениватель ставит всем моделям примерно одинаковые оценки, не различая их качество.

---

### 4. Verbosity Bias (Смещение к длине ответа)

#### Описание
Показывает, предпочитает ли оцениватель длинные или короткие ответы, независимо от их качества.

#### Расчет

**Verbosity Score для каждой модели:**
- Модели сортируются по длине ответа (от короткого к длинному)
- Самый короткий получает 0.0, самый длинный - 1.0
- Промежуточные значения распределяются равномерно (0.333, 0.667)

**Метрики:**
1. `verbosity_first` - средний verbosity score модели на 1-м месте
2. `verbosity_weighted` - взвешенный verbosity (учитывает все позиции с весами 4,3,2,1)
3. `verbosity_correlation` - корреляция между длиной ответа и рангом (инвертированная)

#### Поля в результатах
- `verbosity_first` - значение от 0.0 до 1.0
- `verbosity_weighted` - значение от 0.0 до 1.0
- `verbosity_correlation` - значение от -1.0 до 1.0

#### Интерпретация

**verbosity_first:**
- **> 0.7** - часто ставит длинные ответы на 1-е место
- **0.3 - 0.7** - нейтрально
- **< 0.3** - часто ставит короткие ответы на 1-е место

**verbosity_weighted:**
- **> 0.7** - сильное предпочтение длинных ответов (🚩 флаг `flag_verbosity_long`)
- **0.3 - 0.7** - нейтрально
- **< 0.3** - сильное предпочтение коротких ответов (🚩 флаг `flag_verbosity_short`)

**verbosity_correlation:**
- **> 0.3** - положительная корреляция: чем длиннее ответ, тем выше ранг
- **≈ 0** - нет связи между длиной и рангом
- **< -0.3** - отрицательная корреляция: чем короче ответ, тем выше ранг

#### Пример
Если `verbosity_weighted = 0.8`, это означает, что оцениватель систематически предпочитает длинные ответы, что может быть смещением, а не объективной оценкой качества.

---

### 5. Дополнительные метрики

#### Model Bias Standard Deviation
- **Поле:** `model_bias_std`
- **Описание:** Стандартное отклонение model bias значений
- **Интерпретация:** Показывает разброс предпочтений оценивателя. Высокое значение (>0.4) означает, что оцениватель сильно различает модели.

#### Preferred Model
- **Поле:** `preferred_model`
- **Описание:** Модель с наивысшим средним баллом у оценивателя
- **Интерпретация:** Какая модель получает наивысшие оценки от этого оценивателя.

#### Least Preferred Model
- **Поле:** `least_preferred_model`
- **Описание:** Модель с наименьшим средним баллом у оценивателя
- **Интерпретация:** Какая модель получает наименьшие оценки от этого оценивателя.

#### Preference Gap
- **Поле:** `preference_gap`
- **Описание:** Разница между максимальным и минимальным model bias
- **Интерпретация:**
  - **< 0.5** - слабое смещение
  - **0.5 - 1.5** - умеренное смещение
  - **> 1.5** - сильное смещение (🚩 флаг `flag_strong_model_bias`)

---

## Флаги (Flags)

Флаги указывают на потенциальные проблемы в оценивании:

### flag_mechanical_pattern
- **Условие:** `monotonicity_score > 0.7`
- **Значение:** Оцениватель использует механический паттерн, ставя модели в одном порядке независимо от качества

### flag_low_variance
- **Условие:** `variance_score < 1.0`
- **Значение:** Оцениватель почти не различает качество ответов, ставит всем примерно одинаковые оценки

### flag_strong_model_bias
- **Условие:** `preference_gap > 1.5`
- **Значение:** Сильное систематическое смещение к определенной модели

### flag_verbosity_long
- **Условие:** `verbosity_weighted > 0.7`
- **Значение:** Сильное предпочтение длинных ответов

### flag_verbosity_short
- **Условие:** `verbosity_weighted < 0.3`
- **Значение:** Сильное предпочтение коротких ответов

### flag_verbosity_bias
- **Условие:** `flag_verbosity_long OR flag_verbosity_short`
- **Значение:** Есть смещение к длине ответа (в любую сторону)

### suspicious
- **Условие:** `flag_mechanical_pattern OR flag_low_variance OR flag_strong_model_bias`
- **Значение:** Общий флаг подозрительного оценивателя, требующего дополнительного внимания

---

## Интерпретация результатов

### Хороший оцениватель
- Model bias значения близки друг к другу (preference_gap < 0.5)
- Monotonicity score умеренный (0.2 - 0.5)
- Variance score около 1.25
- Verbosity bias нейтральный (0.4 - 0.6)
- Нет поднятых флагов

### Проблемный оцениватель
- Сильное смещение к одной модели (preference_gap > 1.5)
- Механический паттерн (monotonicity_score > 0.7)
- Низкая дисперсия (variance_score < 1.0)
- Сильное предпочтение длины ответа (verbosity_weighted < 0.3 или > 0.7)
- Поднят флаг `suspicious`

### Примеры интерпретации

**Пример 1: Объективный оцениватель**
```
model_bias_Gemini: 2.4
model_bias_DeepSeek: 2.5
model_bias_Flash: 2.3
model_bias_Qwen: 2.6
preference_gap: 0.3
monotonicity_score: 0.3
variance_score: 1.25
verbosity_weighted: 0.5
suspicious: False
```
→ Оцениватель относительно объективен, небольшое предпочтение Qwen, но в пределах нормы.

**Пример 2: Сильное смещение**
```
model_bias_Gemini: 1.5
model_bias_DeepSeek: 1.8
model_bias_Flash: 2.0
model_bias_Qwen: 3.5
preference_gap: 2.0
monotonicity_score: 0.8
variance_score: 1.25
suspicious: True
```
→ Сильное систематическое смещение к Qwen, механический паттерн оценивания.

**Пример 3: Предпочтение длины**
```
model_bias_Gemini: 2.2
model_bias_DeepSeek: 2.8
model_bias_Flash: 2.1
model_bias_Qwen: 2.9
verbosity_weighted: 0.85
flag_verbosity_long: True
```
→ Оцениватель сильно предпочитает длинные ответы, что может быть смещением.

---

## Использование результатов

1. **Идентификация проблемных оценивателей** - используйте флаг `suspicious` для фильтрации
2. **Анализ систематических смещений** - смотрите на `preference_gap` и `preferred_model`
3. **Проверка объективности** - анализируйте `monotonicity_score` и `variance_score`
4. **Учет длины ответа** - проверяйте `verbosity_bias` метрики

---

## Технические детали

### Алгоритм построения ранжирования

Для каждой задачи и каждого критерия:
1. Собираются оценки всех 4 моделей
2. Модели сортируются по убыванию оценки
3. Строится ранжирование: [модель_1-го_места, модель_2-го_места, модель_3-го_места, модель_4-го_места]

### Преобразование оценок в баллы

- Оценка 1.0 → Ранг 1 → Балл 4
- Оценка 0.6666666667 → Ранг 2 → Балл 3
- Оценка 0.3333333333 → Ранг 3 → Балл 2
- Оценка 0.0 → Ранг 4 → Балл 1

### Расчет verbosity score

1. Для каждой задачи извлекаются длины текстов всех моделей
2. Модели сортируются по длине (от короткой к длинной)
3. Присваиваются verbosity scores: 0.0, 0.333, 0.667, 1.0
4. Вычисляются метрики на основе этих scores и рангов моделей

---

## Ссылки

- Исходный скрипт: `analyze_llm_bias.py`
- Результаты анализа: `llm_bias_analysis_results.csv`
- Исходный алгоритм: `bias/analyze_bias.py` (для человеческих оценивателей)
